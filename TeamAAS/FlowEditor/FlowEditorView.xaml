<UserControl x:Class="TeamAAS.FlowEditor.FlowEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:fe="clr-namespace:TeamAAS.FlowEditor.Controls">

    <UserControl.Resources>
        <!-- 画布网格背景（透明底 + 浅灰网格线，叠加在 SecondaryBorderBrush 上） -->
        <DrawingBrush x:Key="GridBrush" TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Geometry="M0,0 L20,0 M0,0 L0,20">
                        <GeometryDrawing.Pen>
                            <Pen Brush="{DynamicResource SecondaryBorderBrush}"  Thickness="0.5"/>
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>

        <!-- MoveThumb 透明模板 -->
        <Style TargetType="{x:Type fe:MoveThumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type fe:MoveThumb}">
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ConnectorControl 连接点样式 -->
        <Style TargetType="{x:Type fe:ConnectorControl}">
            <Setter Property="Width" Value="14"/>
            <Setter Property="Height" Value="14"/>
            <Setter Property="Cursor" Value="Cross"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type fe:ConnectorControl}">
                        <Grid Width="14" Height="14">
                            <Ellipse Fill="Transparent" Margin="-8"/>
                            <Ellipse Width="10" Height="10"
                                     Fill="{DynamicResource PrimaryBrush}"
                                     Stroke="#FFFFFF" StrokeThickness="1.5"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- NodeControl 节点样式 -->
        <Style TargetType="{x:Type fe:NodeControl}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type fe:NodeControl}">
                        <Grid>
                            <!-- ========== 矩形节点（Normal/ToolBlock） ========== -->
                            <Border x:Name="RectNode" CornerRadius="4"
                                    BorderBrush="{DynamicResource PrimaryBrush}"
                                    BorderThickness="2"
                                    Background="{DynamicResource RegionBrush}"
                                    MinHeight="35"
                                    ClipToBounds="True">
                                <StackPanel Orientation="Horizontal">
                                    <!-- 左侧色条 + 图标 -->
                                    <Border Background="{DynamicResource PrimaryBrush}"
                                            CornerRadius="3,0,0,3" Width="32">
                                        <Path Width="16" Height="16" Stretch="Uniform"
                                              Data="{Binding IconGeometry}" Fill="White"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                    </Border>
                                    <!-- 中间：名称 + 耗时 -->
                                    <StackPanel VerticalAlignment="Center" Margin="8,0,8,0" MinWidth="100">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding NodeName}"
                                                       Foreground="{DynamicResource PrimaryTextBrush}"
                                                       FontSize="13"
                                                       FontFamily="Microsoft YaHei"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding CostTimeText}"
                                                       MinWidth="20"
                                                       Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11"
                                                       Margin="8,0,0,0"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <!-- 右侧色条 + 状态圆 -->
                                    <Border Background="{DynamicResource PrimaryBrush}"
                                            CornerRadius="0,3,3,0" Width="20">
                                        <Ellipse Width="10" Height="10"
                                                 Fill="{Binding StatusColor}"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- ========== 循环节点（ForLoop） ========== -->
                            <Border x:Name="LoopNode" Visibility="Collapsed"
                                    MinWidth="200" MinHeight="35"
                                    CornerRadius="4"
                                    BorderBrush="{DynamicResource DarkInfoBrush}"
                                    BorderThickness="0"
                                    Background="{DynamicResource RegionBrush}"
                                    ClipToBounds="True">
                                <StackPanel>
                                    <!-- 头部色条 -->
                                    <Border Background="{DynamicResource DarkInfoBrush}"
                                            CornerRadius="3,3,0,0" Padding="8,4">
                                        <TextBlock Text="循环" Foreground="White"
                                                   FontSize="11" FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   FontFamily="Microsoft YaHei"/>
                                    </Border>
                                    <!-- 内容 -->
                                    <StackPanel Orientation="Horizontal" Margin="8,6,6,6">
                                        <Path Width="14" Height="14" Stretch="Uniform"
                                              Data="{Binding IconGeometry}"
                                              Fill="{DynamicResource DarkInfoBrush}"
                                              VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding NodeName}"
                                                   Foreground="{DynamicResource PrimaryTextBrush}"
                                                   FontSize="13"
                                                   FontFamily="Microsoft YaHei"
                                                   Margin="6,0,0,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding CostTimeText}"
                                                   Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11"
                                                   Margin="8,0,0,0"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- ========== 菱形节点（Decision） ========== -->
                            <Grid x:Name="DiamondNode" Visibility="Collapsed"
                                  Width="180" Height="56">
                                <Path x:Name="DiamondPath"
                                      Data="M0.12,0 L0.88,0 L1,0.5 L0.88,1 L0.12,1 L0,0.5 Z"
                                      Stretch="Fill"
                                      Fill="{DynamicResource RegionBrush}"
                                      Stroke="{DynamicResource DarkWarningBrush}"
                                      StrokeThickness="1.5"/>
                                <StackPanel HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="30,4">
                                    <TextBlock Text="{Binding NodeName}"
                                               Foreground="{DynamicResource PrimaryTextBrush}"
                                               FontSize="12"
                                               FontFamily="Microsoft YaHei"
                                               HorizontalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding CostTimeText}"
                                               Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10"
                                               HorizontalAlignment="Center"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>

                            <!-- 拖拽手柄 -->
                            <fe:MoveThumb Cursor="Hand" Panel.ZIndex="1"/>

                            <!-- 连接点容器（常驻可见，悬停高亮） -->
                            <Grid x:Name="ConnectorContainer"
                                  Opacity="0" Panel.ZIndex="2">
                                <!-- 左 -->
                                <fe:ConnectorControl PortIndex="0" Side="Left"
                                                     VerticalAlignment="Center"
                                                     HorizontalAlignment="Left"
                                                     Margin="-7,0,0,0"/>
                                <!-- 右 -->
                                <fe:ConnectorControl PortIndex="1" Side="Right"
                                                     VerticalAlignment="Center"
                                                     HorizontalAlignment="Right"
                                                     Margin="0,0,-7,0"/>
                                <!-- 上 -->
                                <fe:ConnectorControl PortIndex="2" Side="Top"
                                                     VerticalAlignment="Top"
                                                     HorizontalAlignment="Center"
                                                     Margin="0,-7,0,0"/>
                                <!-- 下 -->
                                <fe:ConnectorControl PortIndex="3" Side="Bottom"
                                                     VerticalAlignment="Bottom"
                                                     HorizontalAlignment="Center"
                                                     Margin="0,0,0,-7"/>
                            </Grid>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- 悬停：高亮连接点 -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ConnectorContainer" Property="Opacity" Value="1"/>
                                <Setter TargetName="RectNode" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="LoopNode" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="DiamondPath" Property="StrokeThickness" Value="2.5"/>
                            </Trigger>
                            <!-- 选中：加粗边框 + 显示连接点 -->
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="RectNode" Property="BorderThickness" Value="2.5"/>
                                <Setter TargetName="LoopNode" Property="BorderThickness" Value="2.5"/>
                                <Setter TargetName="DiamondPath" Property="StrokeThickness" Value="3"/>
                                <Setter TargetName="ConnectorContainer" Property="Opacity" Value="1"/>
                            </DataTrigger>
                            <!-- Decision → 菱形 -->
                            <DataTrigger Binding="{Binding Category}" Value="Decision">
                                <Setter TargetName="RectNode" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="LoopNode" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="DiamondNode" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <!-- ForLoop → 循环头 -->
                            <DataTrigger Binding="{Binding Category}" Value="ForLoop">
                                <Setter TargetName="RectNode" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="LoopNode" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="DiamondNode" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 工具箱项样式 -->
        <Style x:Key="ToolboxItem" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource RegionBrush}"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource SecondaryBorderBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="160" MinHeight="80" MaxHeight="400"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="220"/>
        </Grid.ColumnDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" Height="36"
                Background="{DynamicResource RegionBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                <TextBlock Text="流程编辑" FontSize="14" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <Button Content="+" Width="28" Height="24" Click="ZoomIn_Click" ToolTip="放大"/>
                <Button Content="−" Width="28" Height="24" Click="ZoomOut_Click" ToolTip="缩小" Margin="2,0"/>
                <TextBlock x:Name="TxtZoom" Text="100%" FontSize="12"
                           Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="8,0"/>
                <Button Content="适应内容" Width="60" Height="24" Click="FitView_Click" ToolTip="缩放至所有节点可见"/>
                <Separator Margin="8,4"/>
                <Button x:Name="BtnRun" Content="▶ 运行" Width="60" Height="24" Click="RunFlow_Click" ToolTip="运行流程"/>
                <Button x:Name="BtnStop" Content="■ 停止" Width="60" Height="24" Click="StopFlow_Click" ToolTip="停止运行" Margin="2,0,0,0" IsEnabled="False"/>
            </StackPanel>
        </Border>

        <!-- 左: 工具箱（跨画布+底部） -->
        <Border Grid.Row="1" Grid.RowSpan="3" Grid.Column="0"
                    Background="{DynamicResource RegionBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="节点工具箱" FontSize="13"
                               FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="12,8,0,4"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ToolboxGroups}" Margin="4,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4,2">
                                        <TextBlock Text="{Binding GroupName}" FontSize="11"
                                                   Foreground="{DynamicResource SecondaryTextBrush}" Margin="4,4,0,2"/>
                                        <ItemsControl ItemsSource="{Binding Items}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource ToolboxItem}"
                                                            MouseLeftButtonDown="ToolboxItem_MouseLeftButtonDown"
                                                            MouseMove="ToolboxItem_MouseMove">
                                                        <StackPanel Orientation="Horizontal">
                                                            <Path Width="14" Height="14" Stretch="Uniform"
                                                                  Data="{Binding IconGeometry}"
                                                                  Fill="{DynamicResource PrimaryTextBrush}"
                                                                  VerticalAlignment="Center"/>
                                                            <StackPanel Margin="8,0,0,0">
                                                                <TextBlock Text="{Binding DisplayName}"
                                                                           FontSize="12"
                                                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                                                <TextBlock Text="{Binding Description}"
                                                                           FontSize="10"
                                                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                                                           TextTrimming="CharacterEllipsis"/>
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- 中: 画布 + 鸟瞰图 -->
            <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Background="{DynamicResource SecondaryRegionBrush}" ClipToBounds="True">
                <fe:FlowCanvas x:Name="EditorCanvas"
                               Background="{StaticResource GridBrush}"
                               ClipToBounds="True"/>
                <fe:MinimapControl x:Name="Minimap"
                                   Width="200" Height="140"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Bottom"
                                   Margin="0,0,8,8"
                                   Canvas="{Binding ElementName=EditorCanvas}"/>
            </Grid>

            <!-- 分割线（可拖拽调节上下） -->
            <GridSplitter Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                          Height="6" HorizontalAlignment="Stretch" ResizeDirection="Rows"
                          Background="{DynamicResource BorderBrush}"
                          ResizeBehavior="PreviousAndNext"/>

            <!-- 底部: 节点信息 + 执行结果表格 + 操作提示 -->
            <Grid Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <!-- 左: 节点信息 -->
                <Border Grid.Column="0"
                        Background="{DynamicResource RegionBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,1,0">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="节点信息" FontSize="13"
                                   FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="12,8,0,4"/>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="12,4">
                                <TextBlock Text="名称" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,8,0,2"/>
                                <TextBlock Text="{Binding SelectedNode.NodeName}" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" TextTrimming="CharacterEllipsis"/>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="类型" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding SelectedNode.Category}" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="状态" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,2"/>
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="8" Height="8" Fill="{Binding SelectedNode.StatusColor}" Margin="0,0,4,0"/>
                                            <TextBlock Text="{Binding SelectedNode.StatusText}" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                                <TextBlock Text="耗时" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,8,0,2"/>
                                <TextBlock Text="{Binding SelectedNode.CostTimeText}" FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <Button Content="编辑属性" Padding="12,4" Click="EditProperties_Click" Margin="0,16,0,0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedNode}" Value="{x:Null}">
                                                    <Setter Property="IsEnabled" Value="False"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <!-- 中: 执行结果表格 -->
                <Border Grid.Column="1"
                        Background="{DynamicResource RegionBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
                    <DockPanel>
                        <DockPanel DockPanel.Dock="Top" Margin="12,6,12,4">
                            <TextBlock Text="执行结果" FontSize="13" FontWeight="Bold"
                                       Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="选中单元格按 Ctrl+C 复制" FontSize="10"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       VerticalAlignment="Center" Margin="16,0,0,0"/>
                        </DockPanel>
                        <DataGrid ItemsSource="{Binding SelectedNode.ResultItems}"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserResizeColumns="True"
                                  SelectionMode="Single"
                                  SelectionUnit="Cell"
                                  ClipboardCopyMode="ExcludeHeader"
                                  BorderThickness="0"
                                  Background="{DynamicResource RegionBrush}"
                                  RowBackground="{DynamicResource RegionBrush}"
                                  AlternatingRowBackground="{DynamicResource SecondaryRegionBrush}"
                                  ColumnHeaderHeight="26"
                                  RowHeight="26"
                                  FontSize="12"
                                  Margin="8,0,8,4">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="键" Binding="{Binding Key}" Width="*"/>
                                <DataGridTextColumn Header="值" Binding="{Binding Value}" Width="3*"/>
                                <DataGridTextColumn Header="类型" Binding="{Binding TypeName}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>

                <!-- 右: 操作提示 -->
                <Border Grid.Column="2"
                        Background="{DynamicResource RegionBrush}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="操作提示" FontSize="13"
                                   FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="12,8,0,4"/>
                        <StackPanel Margin="12,4">
                            <TextBlock Text="拖拽工具箱项到画布创建节点" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" TextWrapping="Wrap" Margin="0,8,0,4"/>
                            <TextBlock Text="拖拽端口连线" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4"/>
                            <TextBlock Text="双击编辑属性" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4"/>
                            <TextBlock Text="滚轮缩放 | 中键平移" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4"/>
                            <TextBlock Text="Ctrl+多选 | Del删除" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4"/>
                            <TextBlock Text="Ctrl+C/V 复制粘贴" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4"/>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>
    </Grid>
</UserControl>
