<UserControl x:Class="TeamAAS.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="用户管理" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
            <Button Content="+ 添加用户" Style="{StaticResource ButtonPrimary}" 
                    HorizontalAlignment="Right" Padding="16,6"/>
        </DockPanel>

        <Border Grid.Row="1" Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="16">
            <DataGrid AutoGenerateColumns="False" CanUserAddRows="False"
                      HeadersVisibility="All" GridLinesVisibility="Horizontal"
                      BorderBrush="{DynamicResource BorderBrush}" RowHeight="36">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="用户名" Width="150"/>
                    <DataGridTextColumn Header="姓名" Width="120"/>
                    <DataGridTextColumn Header="角色" Width="100"/>
                    <DataGridTextColumn Header="最后登录" Width="180"/>
                    <DataGridTextColumn Header="状态" Width="80"/>
                    <DataGridTextColumn Header="操作" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
