<UserControl x:Class="TeamAAS.Views.CalibrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="标定向导" FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,16"/>

        <!-- Step Indicator -->
        <hc:StepBar Grid.Row="1" StepIndex="0" Margin="0,0,0,20">
            <hc:StepBarItem Content="九点标定"/>
            <hc:StepBarItem Content="旋转中心"/>
            <hc:StepBarItem Content="验证"/>
            <hc:StepBarItem Content="完成"/>
        </hc:StepBar>

        <!-- Content -->
        <Border Grid.Row="2" Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="24">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="标定内容区" FontSize="20" Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="请按照步骤完成标定操作" FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Navigation -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button Content="上一步" Padding="24,8" Margin="0,0,12,0"/>
            <Button Content="下一步" Style="{StaticResource ButtonPrimary}" Padding="24,8"/>
        </StackPanel>
    </Grid>
</UserControl>
