<UserControl x:Class="TeamAAS.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Navigation -->
        <Border Grid.Column="0" Background="{DynamicResource RegionBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <StackPanel Margin="8,12">
                <TextBlock Text="设备管理" FontSize="14" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>
                <ListBox Background="Transparent" BorderThickness="0" SelectedIndex="0">
                    <ListBoxItem Content="📷 相机管理" Padding="12,8"/>
                    <ListBoxItem Content="🤖 机器人" Padding="12,8"/>
                    <ListBoxItem Content="📡 PLC" Padding="12,8"/>
                    <ListBoxItem Content="💡 光源" Padding="12,8"/>
                    <ListBoxItem Content="📦 供料器" Padding="12,8"/>
                    <ListBoxItem Content="🔌 通信" Padding="12,8"/>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Right: Device Table -->
        <Grid Grid.Column="1" Margin="16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="相机管理" FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="+ 添加" Style="{StaticResource ButtonPrimary}" Padding="16,4" Margin="0,0,8,0"/>
                    <Button Content="删除" Padding="16,4"/>
                </StackPanel>
            </DockPanel>

            <!-- DataGrid -->
            <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False"
                      HeadersVisibility="All" GridLinesVisibility="Horizontal"
                      BorderBrush="{DynamicResource BorderBrush}" RowHeight="36">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="序号" Width="60" IsReadOnly="True"/>
                    <DataGridTextColumn Header="名称" Width="120"/>
                    <DataGridTextColumn Header="品牌" Width="100"/>
                    <DataGridTextColumn Header="IP地址" Width="140"/>
                    <DataGridTextColumn Header="状态" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="操作" Width="120" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Config Details -->
            <Border Grid.Row="2" Background="{DynamicResource SecondaryRegionBrush}" CornerRadius="8" 
                    Padding="16,10" Margin="0,12,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="分辨率: 1920x1080" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,24,0"/>
                    <TextBlock Text="曝光: 5000us" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,24,0"/>
                    <TextBlock Text="帧率: 30fps" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,24,0"/>
                    <TextBlock Text="触发: 软触发" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
