<UserControl x:Class="TeamAAS.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="16,12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="65*"/>
            <ColumnDefinition Width="35*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Camera Display + Product Selection -->
        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Product Selection Bar -->
            <Border Grid.Row="0" Background="{DynamicResource SecondaryRegionBrush}" 
                    CornerRadius="8" Padding="12,8" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="产品选择:" VerticalAlignment="Center" 
                               Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                    <ComboBox Width="180" SelectedIndex="0" hc:InfoElement.Placeholder="选择产品">
                        <ComboBoxItem Content="A. 产品A"/>
                        <ComboBoxItem Content="B. 产品B"/>
                    </ComboBox>
                    <Button Content="加载" Style="{StaticResource ButtonPrimary}" Margin="8,0,0,0" Padding="16,4"/>
                </StackPanel>
            </Border>

            <!-- Camera Display Placeholder -->
            <Border Grid.Row="1" Background="{DynamicResource SecondaryRegionBrush}" CornerRadius="8">
                <Grid>
                    <TextBlock Text="相机画面显示区" HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="24" Foreground="{DynamicResource SecondaryTextBrush}"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Right: Stats + Logs -->
        <Grid Grid.Column="1" Margin="8,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Production Stats -->
            <Border Grid.Row="0" Background="{DynamicResource RegionBrush}" CornerRadius="8" 
                    Padding="16,12" Margin="0,0,0,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="总产量" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                            <TextBlock Text="15,832" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock Text="今日产量" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                            <TextBlock Text="1,247" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                        </StackPanel>
                    </Grid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                        <TextBlock Text="UPH: " Foreground="{DynamicResource SecondaryTextBrush}" FontSize="13"/>
                        <TextBlock Text="326" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <TextBlock Text="  |  CT: " Foreground="{DynamicResource SecondaryTextBrush}" FontSize="13"/>
                        <TextBlock Text="8.52s" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                    </StackPanel>
                    <Button Grid.Row="2" Content="清零" HorizontalAlignment="Center" Margin="0,8,0,0" 
                            Padding="20,4" FontSize="12"/>
                </Grid>
            </Border>

            <!-- Running Logs -->
            <Border Grid.Row="1" Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="运行信息" FontSize="14" FontWeight="Bold" 
                               Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,8"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="[11:05] 机器人1到位" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,2"/>
                            <TextBlock Text="[11:05] 拍照完成" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,2"/>
                            <TextBlock Text="[11:06] 检测OK" Foreground="#4CAF50" FontSize="12" Margin="0,2"/>
                            <TextBlock Text="[11:06] PLC写入完成" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,2"/>
                            <TextBlock Text="[11:07] 机器人1到位" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,2"/>
                            <TextBlock Text="[11:07] 拍照完成" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,2"/>
                            <TextBlock Text="[11:08] 检测NG" Foreground="#F44336" FontSize="12" Margin="0,2"/>
                            <TextBlock Text="[11:08] 报警丢弃" Foreground="#FF9800" FontSize="12" Margin="0,2"/>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
