<UserControl x:Class="TeamAAS.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="产量统计" FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,16"/>

        <!-- Summary Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <Border Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="16,12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="总产量" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                    <TextBlock Text="15,832" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="16,12" Margin="8,0,8,0">
                <StackPanel>
                    <TextBlock Text="OK数" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                    <TextBlock Text="15,201" FontSize="24" FontWeight="Bold" Foreground="#4CAF50"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="16,12" Margin="8,0,8,0">
                <StackPanel>
                    <TextBlock Text="NG数" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                    <TextBlock Text="631" FontSize="24" FontWeight="Bold" Foreground="#F44336"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="16,12" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="良率" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                    <TextBlock Text="96.0%" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Chart Placeholder -->
        <Border Grid.Row="2" Background="{DynamicResource RegionBrush}" CornerRadius="8" Padding="24">
            <StackPanel>
                <TextBlock Text="产量趋势图" FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>
                <Border Background="{DynamicResource SecondaryRegionBrush}" CornerRadius="4" 
                        Height="300" VerticalAlignment="Stretch">
                    <TextBlock Text="图表区域" HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="20" Foreground="{DynamicResource SecondaryTextBrush}"/>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
